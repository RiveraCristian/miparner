<?xml version="1.0" encoding="utf-8"?>
<!--
  Politica de red de la app.

  Android bloquea el trafico sin cifrar desde la version 9 (API 28). En las
  compilaciones de debug el manifiesto lo permite para todo; en release no, y
  por eso una build de release no alcanzaba al backend local.

  Aqui la excepcion es minima y deliberada: solo el host del emulador
  (10.0.2.2 es la maquina anfitriona vista desde el emulador) y localhost.
  Cualquier otro destino, incluido el servidor de produccion, sigue obligado
  a HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
