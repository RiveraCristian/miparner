import { PantallaDocumentos } from "../../../../shared/Documentos";

/** Documentos de validación. La pantalla es común a las dos apps. */
export function DocumentosScreen() {
  return <PantallaDocumentos />;
}
